import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const states = {};

const mutations = {};

const actions = {};

const getters = {};

export default new Vuex.Store({
    state: states,
    mutations,
    actions,
    getters,
});
